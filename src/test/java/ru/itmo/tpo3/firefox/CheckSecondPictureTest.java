package ru.itmo.tpo3.firefox;// Generated by Selenium IDE

import org.junit.After;
import org.junit.Before;
import org.junit.Test;
import org.openqa.selenium.*;
import org.openqa.selenium.firefox.FirefoxDriver;

import java.util.HashMap;
import java.util.List;
import java.util.Map;

public class CheckSecondPictureTest {
  private WebDriver driver;
  private Map<String, Object> vars;
  JavascriptExecutor js;
  @Before
  public void setUp() {
    System.setProperty("webdriver.gecko.driver", "C:\\drivers\\geckodriver.exe");
    System.setProperty("webdriver.firefox.marionette","D:\\drivers\\geckodriver.exe");
    driver = new FirefoxDriver();
    js = (JavascriptExecutor) driver;
    vars = new HashMap<String, Object>();
  }
  @After
  public void tearDown() {
    driver.quit();
  }
  @Test
  public void checkSecondPicture() {
    driver.get("http://angely-sveta.ru/russian/default_2_ru.htm");
    driver.manage().window().setSize(new Dimension(866, 622));
    {
      List<WebElement> elements = driver.findElements(By.xpath("//img[@alt=\' Next \']"));
      assert(elements.size() > 0);
    }
    driver.close();
  }
}
