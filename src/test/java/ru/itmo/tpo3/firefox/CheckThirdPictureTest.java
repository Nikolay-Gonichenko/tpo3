package ru.itmo.tpo3.firefox;// Generated by Selenium IDE

import org.junit.After;
import org.junit.Before;
import org.junit.Test;
import org.openqa.selenium.*;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.firefox.FirefoxDriver;

import java.util.HashMap;
import java.util.List;
import java.util.Map;

public class CheckThirdPictureTest {
  private WebDriver driver;
  private Map<String, Object> vars;
  JavascriptExecutor js;
  @Before
  public void setUp() {
    System.setProperty("webdriver.gecko.driver", "C:\\drivers\\geckodriver.exe");
    System.setProperty("webdriver.firefox.marionette","D:\\drivers\\geckodriver.exe");
    driver = new FirefoxDriver();
    js = (JavascriptExecutor) driver;
    vars = new HashMap<String, Object>();
  }
  @After
  public void tearDown() {
    driver.quit();
  }
  @Test
  public void checkThirdPicture() {
    driver.get("http://angely-sveta.ru/russian/");
    driver.manage().window().setSize(new Dimension(870, 626));
    driver.findElement(By.cssSelector("img")).click();
    driver.findElement(By.cssSelector("img")).click();
    {
      List<WebElement> elements = driver.findElements(By.xpath("//img[@alt=\' Next \']"));
      assert(elements.size() > 0);
    }
  }
}
